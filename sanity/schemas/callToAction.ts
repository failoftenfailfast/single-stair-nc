import { defineType } from 'sanity';

export default defineType({
  name: 'callToAction',
  title: 'Call to Action',
  type: 'document',
  fields: [
    {
      name: 'title',
      title: 'Title',
      type: 'string',
    },
  ],
});










