import { defineType } from 'sanity';

export default defineType({
  name: 'buildingExample',
  title: 'Building Example',
  type: 'document',
  fields: [
    {
      name: 'name',
      title: 'Name',
      type: 'string',
    },
  ],
});










